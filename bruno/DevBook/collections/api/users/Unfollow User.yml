info:
  name: Unfollow User
  type: http
  seq: 7

http:
  method: POST
  url: "{{API_URL}}/users/:id/unfollow"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{AccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Delete User

  Deletes an existing user identified by their UUID in the URL path.

  **Auth required:** Yes (Bearer token)

  A user can only delete their own record.
