info:
  name: Delete User
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{API_URL}}/users/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
