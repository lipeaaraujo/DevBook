info:
  name: Get Users
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/users?user=a"
  params:
    - name: user
      value: a
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
