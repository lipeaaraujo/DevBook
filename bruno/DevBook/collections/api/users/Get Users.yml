info:
  name: Get Users
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/users?user=t"
  params:
    - name: user
      value: t
      type: query
  auth:
    type: bearer
    token: "{{AccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
