info:
  name: Get User By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_URL}}/users/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
