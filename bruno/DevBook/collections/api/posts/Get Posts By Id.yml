info:
  name: Get Posts By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_URL}}/post/:id"
  params:
    - name: id
      value: 970b45ba-e898-4c72-82e4-f8dfbb9a1380
      type: path
  auth:
    type: bearer
    token: "{{AccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Create User

  Registers a new user. The password is hashed before being stored.

  **Auth required:** No

  ## Request body

  | Field    | Type   | Description                  |
  |----------|--------|------------------------------|
  | name     | string | Full name                    |
  | nickname | string | Display nickname             |
  | email    | string | Unique, valid email address  |
  | password | string | Plain-text password to hash  |

  All fields are required and validated. The email must be unique and
  in a valid format.
