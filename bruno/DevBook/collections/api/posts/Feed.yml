info:
  name: Feed
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/feed"
  auth:
    type: bearer
    token: "{{AccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
