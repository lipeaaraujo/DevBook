#!/bin/sh
if ! head -n1 "$1" | grep -qE '^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\([a-z0-9./-]+\))?!?: .+'; then
	echo "commit message must follow conventional commits: type(scope): subject" >&2
	echo "  e.g. feat(server): add job priorities" >&2
	exit 1
fi
